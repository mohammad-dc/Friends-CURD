<div class="jumbotron">
    <h1 class="display-4"><%= @frinde.first_name %> <%= @frinde.last_name %></h1>
    <p class="lead"> <%= @frinde.email %> | <%= @frinde.phone %> | <%= @frinde.twitter %> </p>
    <hr class="my-4" />
    <%= link_to 'Edit', edit_frinde_path(@frinde), class: "btn btn-secondary" %>
    <%= link_to 'Back', frindes_path, class: "btn btn-dark" %>
</div>